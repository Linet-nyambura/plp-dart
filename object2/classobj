// Student class
class Student {
  String name;
  int age;
  int gradeLevel;

  Student(this.name, this.age, this.gradeLevel);

  void display() {
    print('A student: $name, Age: $age, Grade Level: $gradeLevel);
  }
}

// Teacher class
class Teacher {
  String name;
  int age;
  String subject;

  Teacher(this.name, this.age, this.subject);

  void display() {
    print('A teacher: $name, Age: $age, Subject: $subject');
  }
}

// Main class
class School {
  void run() {
    // Create a student object
    var student = Student('Lucy Kabaki', 13, 8);
    // Create a teacher object
    var teacher = Teacher('Linet Nganga', '30', 'English');

    // Display student information
    student.display();
    // Display teacher information
    teacher.display();
  }
}

void main() {
  // Create a School object
  var school = School();
  // Call the run method to display student and teacher information
  school.run();
}
